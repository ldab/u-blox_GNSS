# Continuous Integration (CI) is the practice, in software
# engineering, of merging all developer working copies with a shared mainline
# several times a day < https://docs.platformio.org/page/ci/index.html >
#
# Documentation:
#
# * Travis CI Embedded Builds with PlatformIO
#   < https://docs.travis-ci.com/user/integration/platformio/ >
#
# * PlatformIO integration with Travis CI
#   < https://docs.platformio.org/page/ci/travis.html >
#
# * User Guide for `platformio ci` command
#   < https://docs.platformio.org/page/userguide/cmd_ci.html >
#

language: python
python:
  - "2.7"

sudo: false
cache:
    directories:
        - "~/.platformio"

env:
    - PLATFORMIO_CI_SRC=examples/LowPower/LowPower.ino
    - PLATFORMIO_CI_SRC=examples/Basic/Basic.ino
    - PLATFORMIO_CI_SRC=examples/Serial_Bridge/Serial_Bridge.ino

install:
    - pip install -U platformio
    - platformio update

script:
    - platformio ci --lib="." --board=d1_mini_pro --board=adafruit_trinket_m0 --board=megaatmega2560
